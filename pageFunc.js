// export function displayHeroes(heroes, heroTable, insertHeaders, insertHeroEntries) {
//     // First we need to clear the table
//     heroTable.innerHTML = '';
//     insertHeaders(heroes);

//     const start = (currentPage - 1) * pageSize;
//     const end = (start + pageSize);
//     const paginatedHeroes = heroes.slice(start, end);

//     paginatedHeroes.forEach(insertHeroEntries);

//     return;
// };

